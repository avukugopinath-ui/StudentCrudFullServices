spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:postgresql://postgres.cre2w8wkw34i.ap-southeast-2.rds.amazonaws.com:5432/postgres
    username: postgres
    password: admin123
  jpa:
    show-sql: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      group-id: student-crud-prod-group

app:
  kafka:
    student-topic: ${STUDENT_TOPIC:student-events-prod}
